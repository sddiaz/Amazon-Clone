"use client";

import NavButton from "../../Shared/NavButton";

const BottomBar = () => {
  return (
    <div>
      <NavButton>Test</NavButton>
      <NavButton>Test</NavButton>
      <NavButton>Test</NavButton>
      <NavButton>Test</NavButton>
    </div>
  );
};

export default BottomBar;
